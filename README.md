# simplifiedJS-library

Make javascript simpler for the dumbarses among us.

takes complex logic like Math.floor(Math.random() * max) + min and turns it into a simpler form, in this case randInt(min,max)
